Sub CoaCalculator()
'
' CoaCalculator Macro
'
' Calculates expected COA based on enrollment, dependency status, housing status, and residency status.
'

Dim i As Integer, j As Integer      ' row, column for ActiveCell
Dim a As Integer, b As Integer      ' row, column for Enrollment
Dim c As Integer, d As Integer      ' row, column for Dependency
Dim e As Integer, f As Integer      ' row, column for Housing
Dim g As Integer, h As Integer      ' row, column for Residency

End Sub
